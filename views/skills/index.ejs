<%-include('../partials/navbar.ejs') %>
<h1>Skills</h1>
<ol>
  <% skills.forEach(s => { %>
  <li>
    <a href="/skills/<%= s.id %>"><%= s.name %></a>
    - <%= s.completed ? 'completed': 'not completed' %>
  </li>
  <% }) %>
</ol>
